
---

> 🔗 [English Version 🇬🇧](env.md)

---

# 🇸🇦 **متغيرات البيئة – نسخة عربية (محدَّثة لمرحلة 10)**

> ⚠️ **اعتبارًا من Phase 10، أصبحت متغيرات DSN هي المصدر الأساسي لتهيئة قواعد البيانات.**
> المتغيرات القديمة (HOST / PORT / USER / PASS) ما زالت مدعومة ولكن أصبح استخدامها “مُهمَّشًا / Deprecated”.

---

## 🧩 **متغيرات البيئة الأساسية (DSN أولًا)**

| المتغير                       | الشرح                                                              |
|-------------------------------|--------------------------------------------------------------------|
| `MYSQL_DSN` / `MYSQL_*_DSN`   | DSN كامل لاتصال MySQL — وهو الأسلوب المفضل الآن.                   |
| `MONGO_DSN` / `MONGO_*_DSN`   | DSN كامل لاتصال MongoDB.                                           |
| `REDIS_DSN` / `REDIS_*_DSN`   | DSN كامل لاتصال Redis.                                             |
| `MYSQL_USER` / `MYSQL_*_USER` | اسم المستخدم (يُستخدم فقط إذا لم يحتوي الـ DSN على بيانات الدخول). |
| `MYSQL_PASS` / `MYSQL_*_PASS` | كلمة مرور MySQL.                                                   |
| `MONGO_USER` / `MONGO_*_USER` | اسم مستخدم MongoDB.                                                |
| `MONGO_PASS` / `MONGO_*_PASS` | كلمة مرور MongoDB.                                                 |
| `REDIS_PASS` / `REDIS_*_PASS` | كلمة مرور Redis (متوافقة مع Phase 10).                             |
| `APP_ENV`                     | بيئة التطبيق (local / testing / production).                       |
| `LOG_PATH`                    | مسار ملفات السجلات العامة.                                         |
| `ADAPTER_LOG_PATH`            | مسار ملفات السجلات الخاصة بالمحولات (Adapters).                    |
| `METRICS_ENABLED`             | تفعيل نظام جمع القياسات (Metrics).                                 |
| `METRICS_EXPORT_FORMAT`       | صيغة إخراج القياسات (`prometheus`, `json`, `none`).                |
| `METRICS_SAMPLING_RATE`       | نسبة العيّنة المأخوذة من الطلبات (0.0 – 1.0).                      |

---

## ⚠️ **متغيرات قديمة (ما زالت مدعومة ولكن تم استبدالها)**

| المتغيرات القديمة                      | البديل الجديد |
|----------------------------------------|---------------|
| `MYSQL_HOST`, `MYSQL_PORT`, `MYSQL_DB` | ← `MYSQL_DSN` |
| `MONGO_HOST`, `MONGO_PORT`, `MONGO_DB` | ← `MONGO_DSN` |
| `REDIS_HOST`, `REDIS_PORT`             | ← `REDIS_DSN` |

---

# 🧠 **مثال لملف `.env` (محدَّث بالكامل لمرحلة 10)**

```env
# ----------------------------------------------------------
# 🔵 محول MySQL (باستخدام DSN)
# ----------------------------------------------------------
MYSQL_MAIN_DSN="mysql:host=127.0.0.1;dbname=maatify_main;charset=utf8mb4"
MYSQL_MAIN_USER=root
MYSQL_MAIN_PASS=

# تهيئة قديمة (Deprecated)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DB=maatify_main
MYSQL_USER=root
MYSQL_PASS=


# ----------------------------------------------------------
# 🟢 محول MongoDB (باستخدام DSN)
# ----------------------------------------------------------
MONGO_LOGS_DSN="mongodb://127.0.0.1:27017/logs"
MONGO_LOGS_USER=
MONGO_LOGS_PASS=
MONGO_LOGS_DB=logs

# تهيئة قديمة (Deprecated)
MONGO_HOST=127.0.0.1
MONGO_PORT=27017
MONGO_DB=logs


# ----------------------------------------------------------
# 🔴 محول Redis (باستخدام DSN)
# ----------------------------------------------------------
REDIS_CACHE_DSN="redis://127.0.0.1:6379"
REDIS_CACHE_PASS=

# تهيئة قديمة (Deprecated)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379


# ----------------------------------------------------------
# ⚙️ الإعدادات العامة للتطبيق
# ----------------------------------------------------------
APP_ENV=local
LOG_PATH=storage/logs
ADAPTER_LOG_PATH=storage/adapter_logs


# ----------------------------------------------------------
# 📊 القياسات والمراقبة
# ----------------------------------------------------------
METRICS_ENABLED=true
METRICS_EXPORT_FORMAT=prometheus
METRICS_SAMPLING_RATE=1.0
```

---

# © 2025 Maatify.dev

مصمم ومطوّر بواسطة **محمد عبدالعليم ([@megyptm](https://github.com/megyptm))** — [https://www.maatify.dev](https://www.maatify.dev)

📘 المستودع الرسمي + التوثيق الكامل:  
[https://github.com/Maatify/data-adapters](https://github.com/Maatify/data-adapters)

---

