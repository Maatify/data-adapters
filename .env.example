# ============================================================================
# 🧱 Maatify Data Adapters – Example Environment Configuration
# ============================================================================
# 🎯 Purpose:
#   Defines ALL supported environment variables for maatify/data-adapters.
#   Includes DSN-based config (Phase 10+) and legacy fallbacks.
#   Fully supports multi-profile adapters.
#
#   Copy/rename to your project:
#       cp examples/.env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# 🔴 REDIS CONFIGURATION
# ----------------------------------------------------------------------------

# Preferred DSN (Phase 10+)
REDIS_DSN=redis://127.0.0.1:6379

# Legacy fallback (only used if REDIS_DSN is missing)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASS=

# ----------------------------------------------------------------------------
# 🔴 REDIS – MULTI PROFILE (redis.cache, redis.analytics…)
# ----------------------------------------------------------------------------
REDIS_CACHE_DSN=redis://:cachePass@127.0.0.1:6379/2
REDIS_CACHE_PASS=cachePass

REDIS_ANALYTICS_DSN=redis://127.0.0.1:6380
REDIS_ANALYTICS_PASS=

# ----------------------------------------------------------------------------
# 🟢 MONGODB CONFIGURATION
# ----------------------------------------------------------------------------

# Preferred DSN
MONGO_DSN=mongodb://127.0.0.1:27017/maatify_dev

# Legacy fallback
MONGO_HOST=127.0.0.1
MONGO_PORT=27017
MONGO_USER=
MONGO_PASS=
MONGO_DB=maatify_dev

# ----------------------------------------------------------------------------
# 🟢 MONGODB – MULTI PROFILE
# ----------------------------------------------------------------------------
MONGO_LOGS_DSN=mongodb://logUser:logPass@127.0.0.1:27017/maatify_logs
MONGO_LOGS_USER=logUser
MONGO_LOGS_PASS=logPass
MONGO_LOGS_DB=maatify_logs

MONGO_ACTIVITY_DSN=mongodb://127.0.0.1:27017/activity
MONGO_ACTIVITY_DB=activity

# ----------------------------------------------------------------------------
# 🔵 MYSQL CONFIGURATION
# ----------------------------------------------------------------------------

# Preferred DSN (Phase 10+)
MYSQL_DSN=mysql:host=127.0.0.1;port=3306;dbname=maatify_dev;;charset=utf8mb4;auth_plugin=caching_sha2_password

# Authentication (shared across ALL profiles unless overridden)
MYSQL_USER=root
MYSQL_PASS=root

# Driver: pdo | dbal
MYSQL_DRIVER=pdo

# Legacy fallback
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DB=maatify_dev

# ----------------------------------------------------------------------------
# 🔵 MYSQL – MULTI PROFILE (mysql.main, mysql.logs, mysql.analytics)
# ----------------------------------------------------------------------------

# --- MAIN PROFILE ---
MYSQL_MAIN_DSN=mysql:host=127.0.0.1;port=3306;dbname=maatify_main;charset=utf8mb4;auth_plugin=caching_sha2_password
MYSQL_MAIN_USER=root
MYSQL_MAIN_PASS=root
MYSQL_MAIN_HOST=127.0.0.1
MYSQL_MAIN_PORT=3306
MYSQL_MAIN_DB=maatify_main

# --- LOGS PROFILE ---
MYSQL_LOGS_DSN=mysql:host=127.0.0.1;port=3306;dbname=maatify_logs;charset=utf8mb4;auth_plugin=caching_sha2_password
MYSQL_LOGS_USER=root
MYSQL_LOGS_PASS=root
MYSQL_LOGS_HOST=127.0.0.1
MYSQL_LOGS_PORT=3306
MYSQL_LOGS_DB=maatify_logs

# ----------------------------------------------------------------------------
# ⚙️ GENERAL APPLICATION SETTINGS
# ----------------------------------------------------------------------------
APP_ENV=local
LOG_PATH=storage/logs
ADAPTER_LOG_PATH=storage/adapter_logs

# ----------------------------------------------------------------------------
# 📊 METRICS & OBSERVABILITY
# ----------------------------------------------------------------------------
METRICS_ENABLED=true
METRICS_EXPORT_FORMAT=prometheus#    # prometheus | json | none
METRICS_SAMPLING_RATE=1.0#           # 0.0 – 1.0

# ============================================================================
# 🔚 End of Example .env
# ============================================================================
# © 2025 Maatify.dev — Engineered by Mohamed Abdulalim (megyptm)
